repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.0
    hooks:
      - id: ruff
        args: [--fix, --line-length=99]
        stages: [pre-commit]
      - id: ruff-format
        stages: [pre-commit]
